#include<iostream>
using namespace std; 
int fact(int n){
    if(n==0){
        return 0;
    }
    else{
        return fact(n-1)*n;
    }
}
int main(){
int r=fact(4);
cout<<" FACTORIAL = "<<r;
return 0;
} 
